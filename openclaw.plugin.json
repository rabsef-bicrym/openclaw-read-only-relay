{
  "id": "read-only-relay",
  "name": "Read-only Relay",
  "description": "Blocks direct replies to configured read-only channel sources and relays them.",
  "activation": {
    "onStartup": true,
    "onCapabilities": ["hook"]
  },
  "uiHints": {
    "enabled": {
      "label": "Enabled",
      "help": "Enable read-only channel relay enforcement."
    },
    "skipRelayToken": {
      "label": "Skip Relay Token",
      "help": "Exact text that suppresses relay when used as the only reply content."
    },
    "promptTemplate": {
      "label": "Prompt Template",
      "help": "Template used to replace the current inbound prompt for model submission. Defaults to {message}."
    },
    "templateEscaping": {
      "label": "Template Escaping",
      "help": "Escaping applied to placeholder values before insertion."
    },
    "rules": {
      "label": "Read-only Rules",
      "help": "Channel endpoints that can send inbound messages but cannot receive direct replies."
    },
    "blockedChannels": {
      "label": "Blocked Channels",
      "help": "Optional channel-wide shorthand for messengers that should be read-only."
    },
    "relay": {
      "label": "Shared Relay",
      "help": "Relay destination used by blockedChannels shorthand rules."
    }
  },
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "enabled": {
        "type": "boolean"
      },
      "skipRelayToken": {
        "type": "string",
        "minLength": 1
      },
      "promptTemplate": {
        "type": "string",
        "minLength": 1
      },
      "templateEscaping": {
        "type": "string",
        "enum": ["none", "xml"]
      },
      "blockedChannels": {
        "type": "array",
        "items": {
          "type": "string",
          "minLength": 1
        }
      },
      "relay": {
        "type": "object",
        "additionalProperties": false,
        "required": ["channel", "to"],
        "properties": {
          "channel": {
            "type": "string",
            "minLength": 1
          },
          "to": {
            "type": "string",
            "minLength": 1
          },
          "accountId": {
            "type": "string",
            "minLength": 1
          },
          "threadId": {
            "type": ["string", "number"]
          }
        }
      },
      "rules": {
        "type": "array",
        "items": {
          "type": "object",
          "additionalProperties": false,
          "required": ["channel", "relay"],
          "properties": {
            "channel": {
              "type": "string",
              "minLength": 1
            },
            "conversationId": {
              "type": "string",
              "minLength": 1
            },
            "accountId": {
              "type": "string",
              "minLength": 1
            },
            "relay": {
              "type": "object",
              "additionalProperties": false,
              "required": ["channel", "to"],
              "properties": {
                "channel": {
                  "type": "string",
                  "minLength": 1
                },
                "to": {
                  "type": "string",
                  "minLength": 1
                },
                "accountId": {
                  "type": "string",
                  "minLength": 1
                },
                "threadId": {
                  "type": ["string", "number"]
                }
              }
            }
          }
        }
      }
    }
  }
}
